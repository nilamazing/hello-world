pipeline{
    agent any
    stages{
        stage('Build code'){
            steps{
                sh '/opt/mvn -f pom.xml clean package'
            }
        }
    }
    post{
        success{
            archiveArtifacts artifacts: '**/*.war'
        }
    }
}